# Snort 3 Anomaly Detection Plugins

These plugins are based on Snort++ Extra plugins. They can detect DoS (DDoS) attack in network traffic.

## Installation:

<pre>
    cd anomaly-plugins
    sudo ./configure_cmake.sh
    cd build
    sudo make install
</pre>